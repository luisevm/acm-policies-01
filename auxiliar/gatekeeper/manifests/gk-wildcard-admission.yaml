# =============================================================================
# Admission monitor: checks for Gatekeeper deny Events for the
# gk-wildcard-clusterrole constraint. Non-compliant when a request has
# been rejected by the Gatekeeper admission webhook (only fires when
# the policy remediationAction is set to enforce / Gatekeeper deny).
# =============================================================================
apiVersion: policy.open-cluster-management.io/v1
kind: ConfigurationPolicy
metadata:
  name: gk-wildcard-admission
spec:
  remediationAction: inform
  severity: high
  object-templates:
    - complianceType: mustnothave
      objectDefinition:
        apiVersion: v1
        kind: Event
        metadata:
          namespace: openshift-gatekeeper-system
          annotations:
            constraint_action: deny
            constraint_kind: K8sWildcardClusterRole
            constraint_name: gk-wildcard-clusterrole
            event_type: violation
